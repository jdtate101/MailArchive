server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Bulk export — disable buffering and extend timeout for large zip streaming
    location /api/export {
        proxy_pass http://mailarchive-backend:8000/api/export;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 3600s;
        proxy_connect_timeout 10s;
        proxy_buffering off;
        proxy_request_buffering off;
        gzip off;
    }

    # Proxy all other API calls to backend service
    location /api/ {
        proxy_pass http://mailarchive-backend:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 300s;
        proxy_connect_timeout 10s;
    }

    # React SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    gzip on;
    gzip_types text/plain text/css application/javascript application/json;
}
